name: Render ACE-Step Backing Track

on:
  push:
    branches: [main]
    paths:
      - 'scripts/render_ace.py'
      - '.github/workflows/render-ace-step.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ffmpeg
          python -m pip install --quiet --upgrade gradio_client yt-dlp

      - name: Download 30-second reference validation clip
        id: download
        continue-on-error: true
        run: |
          mkdir -p generated
          rm -f source.*
          set -o pipefail
          yt-dlp --no-playlist --js-runtimes node --extractor-args 'youtube:player_client=web,android,ios' --download-sections '*0-30' -x --audio-format mp3 --audio-quality 0 -o 'source.%(ext)s' 'https://youtu.be/JG0-eJ4aGOY?si=uBTyWwp4ClfWdZEQ' 2>&1 | tee generated/source_download_log.txt
          test -f source.mp3
          ls -lh source.mp3 | tee -a generated/source_download_log.txt

      - name: Neural render validation
        id: render
        if: steps.download.outcome == 'success'
        continue-on-error: true
        env:
          AURA_SOURCE: source.mp3
          AURA_OUTDIR: generated
          AURA_DURATION: '30'
          AURA_MODEL: acestep-v15-xl-turbo
          AURA_COVER_STRENGTH: '0.72'
        run: |
          set -o pipefail
          python scripts/render_ace.py 2>&1 | tee generated/render_log.txt

      - name: Record status
        if: always()
        run: |
          echo "download=${{ steps.download.outcome }}" > generated/render_status.txt
          echo "render=${{ steps.render.outcome }}" >> generated/render_status.txt
          date -u +'%Y-%m-%dT%H:%M:%SZ' >> generated/render_status.txt

      - name: Commit generated validation result and logs
        if: always()
        run: |
          git config user.name "Aura Music Studio Bot"
          git config user.email "actions@users.noreply.github.com"
          git add generated/
          git commit -m "ACE-Step neural render validation diagnostics" || exit 0
          git push
